#!{{ lookPath "bash" }}

# plugins hash: {{ include "dot_config/yazi/package.toml" | sha256sum }}

if ! command -v ya >/dev/null 2>&1; then
  echo "'ya' is not installed; skipping yazi plugin install"
  exit 0
fi

ya pkg install
